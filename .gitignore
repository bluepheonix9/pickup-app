node_modules/
.expo/
.env

# Native projects are generated from app.json (CNG) — run `npx expo prebuild`
# locally, or let EAS Build generate them. Committing these makes the config
# plugins in app.json inert, which silently drops permission strings.
/ios
/android

# Local MCP server config (no secrets, but machine-specific).
.mcp.json

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
